import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.shivoham.studytracker',
  appName: 'Study Tracker',
  webDir: 'dist'
};

export default config;
